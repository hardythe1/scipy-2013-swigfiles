
class Complex {
private:
  double rpart, ipart;
public:
  Complex();
  Complex(double r, double i);
  Complex(const Complex &c);

  Complex operator+(const Complex &c) const;
  
  double re() const;
  double im() const;
};

